<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.lebogang.genesis">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission
        android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        tools:ignore="ScopedStorage" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />

    <application
        android:name=".GenesisApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:requestLegacyExternalStorage="true"
        android:supportsRtl="true"
        android:theme="@style/Theme.Genesis20.System">

        <service
            android:name=".service.MusicService"
            android:enabled="true"
            android:exported="false" />

        <activity
            android:name=".ui.StatisticsActivity"
            android:parentActivityName=".ui.MainActivity"
            android:launchMode="singleInstance" />
        <activity
            android:name=".ui.AboutActivity"
            android:parentActivityName=".ui.MainActivity"
            android:launchMode="singleInstance" />
        <activity
            android:name=".ui.PlayerActivity"
            android:parentActivityName=".ui.MainActivity"
            android:launchMode="singleInstance" />
        <activity
            android:name=".ui.SettingsActivity"
            android:parentActivityName=".ui.MainActivity"
            android:launchMode="singleInstance" />
        <activity
            android:name=".ui.InfoActivity"
            android:parentActivityName=".ui.MainActivity"
            android:launchMode="singleInstance" />
        <activity
            android:name=".ui.EditAudioActivity"
            android:parentActivityName=".ui.MainActivity"
            android:launchMode="singleInstance" />
        <activity
            android:name=".ui.PlaylistViewActivity"
            android:parentActivityName=".ui.MainActivity"
            android:launchMode="singleInstance" />
        <activity
            android:name=".ui.ArtistViewActivity"
            android:parentActivityName=".ui.MainActivity"
            android:launchMode="singleInstance" />
        <activity
            android:name=".ui.AlbumViewActivity"
            android:parentActivityName=".ui.MainActivity"
            android:launchMode="singleInstance" />
        <activity
            android:name=".ui.MainActivity"
            android:launchMode="singleInstance">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>